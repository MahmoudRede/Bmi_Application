import 'package:flutter/material.dart';

class Favoirets extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Favoirets',style: TextStyle(fontSize: 30),
      ),
    ) ;
  }
}